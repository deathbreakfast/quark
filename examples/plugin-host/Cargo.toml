[package]
name = "plugin-host"
version = "0.1.0"
edition = "2021"
publish = false
description = "Example: cross-crate plugin discovery via quark::Registry"

[[bin]]
name = "plugin-host"
path = "src/main.rs"

[dependencies]
quark = { package = "uf-quark", path = "../.." }
plugin-descriptors = { path = "../plugin-descriptors" }
